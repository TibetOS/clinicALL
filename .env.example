# ClinicALL Environment Variables
# Copy this file to .env.local and fill in your values

# ===========================================
# REQUIRED - Supabase Configuration
# ===========================================
# Get these from your Supabase project settings > API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# ===========================================
# OPTIONAL - Google Gemini AI (SERVER-SIDE ONLY)
# ===========================================
# SECURITY WARNING: Do NOT use VITE_GEMINI_API_KEY in production!
# Any VITE_ prefixed variable is exposed in the client bundle.
#
# For production AI features:
# 1. Use Supabase Edge Functions to proxy API calls
# 2. Store the API key as a Supabase secret (not VITE_ prefixed)
# 3. Or use a separate backend service
#
# This variable is only for local development/testing.
# VITE_GEMINI_API_KEY=your-gemini-api-key-for-dev-only

# ===========================================
# DEVELOPMENT ONLY - Demo Mode
# ===========================================
# WARNING: NEVER set this in production!
# This enables mock data mode when Supabase is not configured
# Only use for local development and demos
# VITE_ALLOW_DEMO_MODE=true
